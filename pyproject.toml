[project]
name = "online-shopping-system"
version = "0.1.0"
description = "Online shopping system domain model"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["src/online_shopping/tests"]
